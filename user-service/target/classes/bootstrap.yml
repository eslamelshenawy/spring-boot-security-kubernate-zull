spring:
  application:
    name: user
  cloud:
    kubernetes:
      config:
        enableApi: true
        paths:
          - /vault/secrets/mysql.properties
      secrets:
        enabled: true
        paths:
          - /etc/secretspot
        enableApi: false